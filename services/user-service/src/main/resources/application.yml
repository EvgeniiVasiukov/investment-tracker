server:
  port: 8080
spring:
  application:
    name: user-service
  datasource:
    username: ${USER_DB_USERNAME}
    password: ${USER_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
jwt:
  secret: ${USER_SERVICE_SECRET_KEY}
  expiration: 3600000